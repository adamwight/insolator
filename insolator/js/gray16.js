Gray64 = function(xres, yres)
{
  var m = new Array(xres * yres);
  function downsample()
  {
    return rgb24;
  }

  function add_fraction(image, divisor = 0x10000)
  {
  }

  function maximum(image)
  {
    return max_value;
  }
};
